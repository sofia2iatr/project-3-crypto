services:
  postgres:
    image: postgres:15
    environment:
      POSTGRES_DB: sofia_crypto
      POSTGRES_USER: sofia
      POSTGRES_PASSWORD: sofia123
    ports:
      - "5433:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata: